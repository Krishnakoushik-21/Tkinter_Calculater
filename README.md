# Dual Mode Calculator 🧮

A modern, responsive web-based calculator featuring a **3D flip interface** that switches between a **Basic** layout and a **Scientific** layout.

## ✨ Features

### 🔄 Dual Interface
- **Basic Mode (Front Face)**: Clean layout for everyday arithmetic.
- **Scientific Mode (Back Face)**: Advanced functions for complex calculations.
- **3D Flip Transition**: Smooth animation when toggling between modes.
- **Synchronized State**: Your current calculation and result are preserved when switching modes.

### 🔢 Capabilities
- **Arithmetic**: Addition (`+`), Subtraction (`-`), Multiplication (`*`), Division (`/`), Modulo (`%`).
- **Trigonometry**: `sin`, `cos`, `tan` (supports both **Degree** and **Radian** modes).
- **Logarithms**: Natural Log (`ln`), Base-10 Log (`log`).
- **Powers & Roots**: Exponentiation (`^`), Square Root (`√`).
- **Constants**: Pi (`π`), Euler's number (`e`).
- **Other**: Factorial (`!`), Inverse (`1/x`), Parentheses `( )` for grouping.

### 🎨 UI/UX
- **Glassmorphism Design**: Frosted glass effect with a vibrant gradient background.
- **Responsive**: Fully functional on desktop and mobile devices.
- **Keyboard Support**: Type directly using your keyboard (Numpad supported).

## 🚀 How to Run

1.  **Clone or Download** the repository.
2.  **Open** the folder `SimpleCalculator`.
3.  **Double-click** on `index.html` to open it in your web browser.

No installation or server is required!

## 🛠️ Tech Stack
- **HTML5**: Semantic structure.
- **CSS3**: Layout (Grid/Flexbox), 3D Transforms, Animations.
- **JavaScript (ES6+)**: Calculation logic, Event handling, DOM manipulation.
- **Google Fonts**: 'Outfit' typeface.
- **FontAwesome**: Icons for the toggle buttons.

## ⌨️ Keyboard Shortcuts
- **0-9**: Enter numbers
- **+ - * /**: Operations
- **Enter** or **=**: Calculate Result
- **Backspace**: Delete last character
- **Esc** or **C**: Clear All
- **( )**: Parentheses
- **^**: Power
- **%**: Modulo

---

